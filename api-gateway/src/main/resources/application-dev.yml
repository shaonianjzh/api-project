spring:
  redis:
    port: 6379
    host: localhost

  cloud:
    gateway:
      routes:
        - id: api-interface
          uri: http://localhost:8123
          predicates:
            - Path=/api/**

dubbo:
  application:
    name: dubbo-consumer
    qos-enable: true
    qos-port: 33333
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://localhost:8848