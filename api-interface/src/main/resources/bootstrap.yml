spring:
  application:
    name: api-interface
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#        namespace:
      config:
        server-addr: localhost:8848
        file-extension: yaml
#        namespace:
server:
  port: 8123
  servlet:
    context-path: /api

